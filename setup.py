from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Introduction to Data Science, Project 1: Jimmy Wrangler, Data Explorer, which loads data sets into panda data frames, transforms the data sets into a single one',
    author='Archana Ramakrishnan, EECS 731',
    license='MIT',
)
